import React from "react";
import { createRoot } from "react-dom/client";

if (document.getElementById("genero") != null) {
    const root = createRoot(getElementById("director"));
    const genero = localStorage.getItem("director");

    root.render(
        
    )
}