{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{% block nav %}
		{{ parent() }}
	{% endblock %}
	<div class="text">
		<div class="container">
			<div class="row">
				<div class="bg-light mt-4 p-4">
					<h1>Registro</h1>

					{{ form_start(registrationForm) }}
					{{ form_row(registrationForm.username,{
                    label: 'Nombre del usuario'
                }) }}
					{{ form_row(registrationForm.plainPassword, {
                    label: 'Contraseña'
                }) }}

					<button type="submit" class="bg-primary btn">Registrar</button>
					{{ form_end(registrationForm) }}
				</div>
			</div>
		</div>
	</div>
	{% block footer %}
		{{ parent()}}
	{% endblock %}

{% endblock %}
